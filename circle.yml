## Customize dependencies
dependencies:
  pre:
    - gem install bundler --pre
    - gem update --system
    - gem install compass
    - npm install -g bower grunt-cli
    - bower install

deployment:
  staging:
    branch: master
    heroku:
      appname: somewherebetweenus-mobile-web

      